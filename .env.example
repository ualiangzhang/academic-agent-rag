# Stage and policy
APP_STAGE=local
POLICY_MODE=strict

# LLM providers and models
LLM_PROVIDER=anthropic
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
LLM_MODEL=anthropic.claude-3-haiku
EMBEDDINGS_MODEL=titan-embed-text-v1

# Vector store / cache
VECTOR_STORE=chroma
VECTOR_DB_URL=
CACHE_URL=

# MCP servers registry (comma-separated)
MCP_SERVERS=rag.retrieve,papers.search,papers.fetch,notes.read,notes.write,db.query,ingest.upload,ingest.extract,ingest.embed
